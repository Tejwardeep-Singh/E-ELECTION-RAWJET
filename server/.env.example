# URL for the private FastAPI service. The browser never calls this service.
AI_SERVICE_URL=http://127.0.0.1:8000
# Registration continues after this many milliseconds if enrollment is unavailable.
AI_SERVICE_TIMEOUT_MS=10000
# Increment when the embedding format/model migration requires it.
FACE_EMBEDDING_VERSION=1
